import React from 'react'
import '../Styles/FPost.css'

// 3 objects { product, images, specs}
//Product = name, description, price, category_id, specs_id, ts, image_id, negotiable, user_id,  transportation
//specs = weight, height, width, length, quantity, unit, color

//price is per what, whole, kg, grams, ?


//const nums = ['LiveStok', 'Fishery', 'AgroChemicals', 'Cash Crops', 'Animal Feeds', 'Fruits', 'Nuts and Kernels', 'Animals', 'Vegetables', 'Tools and Equipmeny', 'Herbs', 'Poultry', 'Food Crop', 'Cerials', 'Animal Feeds', 'Hive Products', 'Flowers', 'Seeds', 'Dairy products', 'Bakery products', 'Other']

//forms = [name, description, price, unit]
//picker = [multiple images]
//select = [category(submit category_id), transportation ]
//add+ = [weight, quantity, color, length, height]

//name, description, price, location, category_id, type, specs_id, ts, image_id, negotiable, user_id, config_id , transportation


const FPost = () => {
  return (
    <div className='FPost'>
      <div className="f-post-form">

      </div>
    </div>
  )
}

export default FPost
